import React from "react";
import Splash from "../components/Splash";

const Home = () => {
    return (
        <div>
            <Splash />
            Home
        </div>
    );
};

export default Home;
